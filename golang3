Faça um algoritmo que leia o peso e a altura de uma pessoa e calcule o seu IMC (Índice de Massa Corporal).

package main

import "fmt"

func main() {
	var peso float64
	var altura float64
	fmt.Println("Informe seu peso em quilogramas")
	fmt.Scan(&peso)
	fmt.Println("Informe sua altura em metros")
	fmt.Scan(&altura)
	var imc = peso / (altura * altura)
	fmt.Println("Seu IMC é", imc)

}
